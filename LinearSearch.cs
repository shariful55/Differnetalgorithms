using System;
using System.Collections.Generic;
using System.Text;

namespace DictionaryPractice
{
    class LinearSearch
    {
        //// C# to search x data linearly in arr[] array
        //// if x exist in arr[] return 1, otherwise return -1
        //static int Linear(int []arr, int x)
        //{
        //    int k = arr.Length;
        //    for (int i = 0; i < k; i++)
        //    {
        //        if (arr[i] == x)
        //            return 1;

        //    }
        //    return -1;
        //}
        //static void( Main)(string[] args)
        //{
        //    int[] sort = { 10, 20, 30, 40, 50, 60, 70, 80, 90 };
        //    int a = 80;
        //    int b = 100;

        //    //function call
        //    int result = Linear(sort, a);
        //    // result- Result is present at Index: 1;
        //    if(result==-1)
        //        Console.WriteLine("Result doesn't exit in index:");
        //    else
        //        Console.WriteLine("Result is present at Index: "+result);
           
            
            
        //    int result1 = Linear(sort, b);
        //    //Result -Result doesn't present in Index: -1
        //    if(result1==-1)
        //        Console.WriteLine("Result doesn't exit in index:");
        //    else
        //        Console.WriteLine("Result is present at Index: "+result1);
        //    Console.ReadKey();

        //}
    }
}
