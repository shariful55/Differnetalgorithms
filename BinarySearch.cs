using System;
using System.Collections.Generic;
using System.Text;

namespace DictionaryPractice
{
    //class BinarySearch
    //{ // c# Implementation of Binary search Algorithms here 
    //    static int binarySearch(int[] arr, int l, int r, int x)
    //    {
    //        if (r >= l)
    //        {
    //            int mid = l + (r - l) / 2;
    //            // if x is present in the array
    //            if (arr[mid] == x)
    //                return mid;

    //            // x is smaller than mid
    //            if (mid > x)
    //                return binarySearch(arr, l, mid - 1, x);
    //            return binarySearch(arr, mid + 1, r, x);

    //        }
    //        return -1;
    //    }
    //    static void Main(string[] args)
    //    {
    //        int[] Code = { 5, 30, 30, 3, 4, 2, 5, };
    //        int n = 5;
    //        int length = Code.Length;

    //        int resutl = binarySearch(Code, 0, length - 1, n);
    //        if (resutl == -1)
    //            Console.WriteLine("Element is not found ");
    //        else
    //            Console.WriteLine("Element is found at index: " + resutl);

        
    //    }
    //}
}
